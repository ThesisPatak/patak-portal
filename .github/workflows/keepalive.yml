name: Keep Render Alive

on:
  schedule:
    - cron: '*/3 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl health endpoint
        run: |
          curl -fsS https://patak-portal-production-351f.up.railway.app/health || true
